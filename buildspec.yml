   version: 0.2

   phases:

     install:
        commands:
           - gem install bundler
           - bundle update
     build:
        commands:
           - bundle exec jekyll build
     post_build:
        commands:
           - aws s3 sync _site/ public s3://www.value4free.de
